<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="@Url.Content("~/Content/styleLogSign.css")" asp-append-version="true" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
    <title>@ViewBag.Title – TechStoreVN</title>
    <style>
        /* Critical auth layout – luôn load kể cả khi file CSS ngoài lỗi */
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html, body.auth-body { height: 100%; }
        body.auth-body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            overflow-x: hidden;
        }
        .auth-page {
            display: flex;
            flex-direction: row;
            min-height: 100vh;
            width: 100%;
        }
        .auth-brand {
            flex: 1 1 45%;
            min-width: 0;
            display: flex;
            align-items: center;
            padding: 64px 72px;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            position: relative;
            overflow: hidden;
        }
        .auth-brand::before {
            content: '';
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at 20% 50%, rgba(102,126,234,.2) 0%, transparent 50%),
                        radial-gradient(circle at 80% 50%, rgba(0,212,255,.15) 0%, transparent 50%);
            pointer-events: none;
        }
        .auth-orb {
            position: absolute; border-radius: 50%; filter: blur(100px);
            animation: orbFloat 20s ease-in-out infinite alternate;
        }
        .auth-orb-1 { width: 400px; height: 400px; background: rgba(102,126,234,.5); top: -100px; left: -80px; }
        .auth-orb-2 { width: 350px; height: 350px; background: rgba(0,212,255,.4); bottom: -80px; right: -60px; animation-delay: -5s; }
        .auth-orb-3 { width: 250px; height: 250px; background: rgba(236,72,153,.3); top: 50%; left: 50%; animation-delay: -10s; }
        
            .auth-brand-inner {
                position: relative;
                z-index: 10;
                color: #ffffff; 
                max-width: 480px;
            }

            /* Xử lý phần Logo */
            .auth-logo {
                display: flex;
                align-items: center;
                gap: 12px;
                margin-bottom: 40px;
            }
            .auth-logo-icon {
                font-size: 32px;
                color: #00d4ff; /* Màu xanh neon cho icon */
            }
            .auth-logo-text {
                font-size: 28px;
                font-weight: 800;
                letter-spacing: -0.5px;
            }
            .auth-logo-text span {
                color: #00d4ff; /* Nhấn mạnh chữ VN */
            }

            /* Xử lý Tiêu đề (h2) */
            .auth-brand-inner h2 {
                font-size: 2.2rem;
                font-weight: 700;
                line-height: 1.3;
                margin-bottom: 16px;
                text-shadow: 0 4px 15px rgba(0,0,0,0.3);
            }
            .auth-brand-inner h2 em {
                font-style: normal;
                color: #ec4899; /* Nhấn mạnh chữ 'đỉnh cao' bằng màu hồng */
            }

            /* Xử lý đoạn văn mô tả (p) */
            .auth-brand-inner p {
                font-size: 1.05rem;
                line-height: 1.6;
                color: rgba(255, 255, 255, 0.75); /* Màu trắng đục nhẹ để làm nền cho tiêu đề */
                margin-bottom: 32px;
            }

            /* Xử lý danh sách tính năng (ul li) */
            .auth-features {
                list-style: none; /* Xóa dấu chấm tròn đen thui */
                padding: 0;
                margin: 0;
                display: flex;
                flex-direction: column;
                gap: 16px;
            }
            .auth-features li {
                display: flex;
                align-items: center;
                gap: 12px;
                font-size: 1rem;
                font-weight: 500;
                color: rgba(255, 255, 255, 0.95);
            }
            .auth-features li i {
                color: #667eea; /* Màu tím pastel cho các icon */
                font-size: 1.2rem;
                width: 24px;
                text-align: center;
            }
        @@keyframes orbFloat { from { transform: translate(0,0); } to { transform: translate(40px,-50px); } }

        .auth-form-panel {
            flex: 1 1 55%;
            min-width: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 48px 32px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            overflow-y: auto;
        }
        .auth-card {
            background: #fff;
            border-radius: 32px;
            box-shadow: 0 24px 80px rgba(0,0,0,.12);
            width: 100%;
            max-width: 500px;
            padding: 48px 44px;
            border: 2px solid rgba(0,0,0,.06);
            transition: all .4s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .auth-card:hover { box-shadow: 0 32px 100px rgba(102,126,234,.15); }
        .auth-card.register-card { max-width: 560px; }
        @@media (max-width: 900px) {
            .auth-page { flex-direction: column; }
            .auth-brand { flex: none; padding: 48px 36px; min-height: auto; }
            .auth-form-panel { flex: 1; padding: 32px 20px 48px; }
            .auth-card { padding: 36px 32px; }
        }
        @@media (max-width: 600px) {
            .auth-brand { padding: 36px 24px; }
            .auth-card { padding: 32px 24px; border-radius: 24px; }
        }
    </style>
</head>
<body class="auth-body">
    @RenderBody()
</body>
</html>
